import React from 'react';
import styled from 'styled-components';
const Line = ({ lineH, lineW, flex, bg }) => {
  return <Rule className="flex h-0.5 bg-primaryColor-white flex-1"></Rule>;
};
const Rule = styled.span``;
export default Line;
